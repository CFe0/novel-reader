# 开发时间线

> 会话开始于 2026-08-16 17:32（北京时间），以下按阶段顺序整理；具体时刻以 `01_对话过程/原始记录/` 中的 JSONL 文件为准。

## 阶段总览

1. **开源调研**（17:32 起）：检索 6 组关键词与指定仓库，GitHub API 拉取 15+ 项目元数据。解决：确认可访问 GitHub，形成借鉴/避坑清单。
2. **补充调研**（17:33 起）：补充 ebook/Tauri/Electron/Flutter 关键词，核查 `xiaocse/Fanqie-novel-Downloader`。解决：输出 A/B/C/D 调研报告与技术选型依据。
3. **形态转向 + 第一版**（17:36 起）：改为浏览器版，搭建 Vite+React+TS，实现编码识别、章节索引、本地书库、起点风格阅读页。解决：满足“Chrome 直接浏览、不用桌面端”。
4. **连续滚动与按钮位置**：正文旁侧栏、章末自动加载下一章、底部按钮居中。解决：免点“下一章”，按钮靠近正文。
5. **布局细化**：侧栏移到右侧并放大；A4 纸张式正文与背景分界；目录两列。解决：更接近起点阅读页观感。
6. **目录调宽 / 翻页模式**：目录拖拽调宽并记忆；设置新增章节/滚动两种阅读模式；滚动模式隐藏按钮。解决：长标题可看全，两种阅读习惯可选。
7. **进度位置与澄清**：进度移到顶栏仅章节模式显示；解释迁移与性能。解决：正文更聚焦，消除迁移/卡顿疑虑。
8. **滚动模式优化**：自动卸载当前章节前 15 章以外的 DOM 与文本，滚动位置补偿。解决：长时间连续阅读不累积内存，弱机器流畅。
9. **进度 Bug 修复**：修复 StrictMode 假卸载覆盖进度，加固恢复逻辑；查证 Chrome 存储路径；首次 git 提交（`1a14efe`）。解决：“关闭重开回到开头”问题。
10. **桌面归档**：生成【小说阅读器】归档包。解决：全部过程、程序、代码、文档集中保存。

## 已实现功能快照（截至归档时）

- 打开 TXT（按钮 / 拖拽 / 选择器），编码自动识别 + 手动切换；
- 章节识别与两列目录（可搜索、可拖宽、宽度记忆）；
- 起点风格阅读页：纸张式正文、首行缩进、正文旁右侧工具（目录/设置/主题色板）；
- 阅读设置：字体、字号、行距、段距、对齐、宽度；四套主题即点即生效；
- 阅读模式：章节翻页 / 滚动翻页（自动加载下一章）；
- 滚动模式自动卸载远章节（保留当前章节前 15 章）；
- 进度自动保存与精确恢复（章节 + 章节内偏移）；
- 本地书架：最近阅读、收藏、全部书籍；
- 单文件构建 `dist/index.html`（Chrome 双击即用）。

## 验证与测试

- `npm run build`（tsc 类型检查 + Vite 生产构建）持续通过；
- 核心逻辑冒烟测试 `npm run test:smoke` 全部通过（编码识别、章节扫描、字节偏移、无章节回退、20MB 大文件 125ms）；
- 开发服务器 HTTP 冒烟验证通过（本会话无浏览器自动化工具，界面视觉由用户实机确认）。
